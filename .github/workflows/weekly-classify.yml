name: weekly-classify
on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch: {}
jobs:
  classify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - name: Setup Configuration Files
        run: |
          cp config/burnrate.sample.yml config/burnrate.yml
          cp config/value_config.sample.yml config/value_config.yml
      - run: npm run classify
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          BURNRATE_CONFIG: config/burnrate.yml
          VALUE_CONFIG_PATH: config/value_config.yml
